<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gjimenezExamen.Views.Login"
             Title="Login">
    
    <VerticalStackLayout
        Padding="25"
        Spacing="20">
        <Label Text="Iniciar Sesión"
               FontSize="20"
               TextColor="Red"
               HorizontalOptions="Center" />
        <Image Source="logo.png" HeightRequest="100" Grid.Row="1" 	HorizontalOptions="Center" />
        <Grid Grid.Row="3" ColumnDefinitions="2">
            <Entry x:Name="usernameEntry" Placeholder="Usuario"/>
            <Entry x:Name="passwordEntry" Placeholder="Contraseña" IsPassword="True" />
            <Button Text="Iniciar sesión" Grid.Row="4" Clicked="OnLoginClicked" />
            <Button Text="Acerca de" Grid.Row="4" Grid.Column="1" Clicked="OnAboutClicked" />
        </Grid>
        <Label x:Name=""Usuario o contraseña incorrectos." 
               TextColor="Red" 
               HorizontalOptions="Center" 
               Grid.Row="5" 
               Grid.ColumnSpan="2" />
    </VerticalStackLayout>
</ContentPage>